# API test placeholder

Add executable API specifications only after the team provides an approved `API_BASE_URL`, an API
contract, authentication rules, and endpoints that are safe to call. Extend `BaseApiClient`; do not
guess production endpoints. The `npm run test:api` command intentionally succeeds when no API tests
exist.
